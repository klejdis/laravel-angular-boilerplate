import { DynamicCmpHostDirective } from './dynamic-cmp-host.directive';

describe('DynamicCmpHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCmpHostDirective();
    expect(directive).toBeTruthy();
  });
});
